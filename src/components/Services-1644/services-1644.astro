---
import { cn } from "@utils/utils";

interface Props {}

const {} = Astro.props;

// Styles
const cardGroup =
  "w-full m-0 p-0 flex flex-col items-stretch sm:flex-row sm:justify-center";
---

<ul class={cn(cardGroup)}>
  <slot />
</ul>
