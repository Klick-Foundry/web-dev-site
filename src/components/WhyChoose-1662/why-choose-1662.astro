---
import { cn } from "@utils/utils";

interface Props {}

const {} = Astro.props;

// Styles
const cardGroup =
  "w-full p-0 m-0 flex justify-center items-start flex-col gap-[clamp(1rem,_2vw,_2rem)]";
---

<ul class={cn(cardGroup)}>
  <slot />
</ul>
